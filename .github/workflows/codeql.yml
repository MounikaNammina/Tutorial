name: SAST Scan
on:
workflow:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  run_scan:
    name: 'SAST Scan'
    uses: .github/workflows/codeql.yml@v3
    with:
      languages: "['java']"
