name: SAST Scan
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  run_scan:
    name: 'SAST Scan'
    uses: ./.github/workflows/codeql.yml
    with:
      languages: "['java']"
