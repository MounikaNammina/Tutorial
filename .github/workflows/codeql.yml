name: SAST Scan
on:
  push:
    branches: [main, codeql]
  pull_request:
    branches: [main, codeql]
jobs:
  run_scan:
    name: 'SAST Scan'
    uses: MounikaNammina/Tutorial/.github/workflows/codeql.yml@main
    with:
      languages: "['java]"
