name: SAST Scan
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  run_scan:
    name: 'SAST Scan'
    uses: MounikaNammina/Tutorial/.github/workflows/codeql.yml@main
    with:
      languages: "['java']"
